import { extendTheme } from "@chakra-ui/react";

const colours = {
  brand: {
    offWhite: "#f2f1f0",
    white: "#fff",
    darkGrey: "#191a1b",
  },
};

export const theme = extendTheme({ colours });
